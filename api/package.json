{
	"name": "api",
	"version": "1.0.0",
	"description": "",
	"scripts": {
		"build": "tsc",
		"watch": "tsc -w",
		"clean": "rimraf dist",
		"prestart": "npm run clean && npm run build",
		"start": "func start",
		"test": "vitest"
	},
	"dependencies": {
		"@azure/functions": "^4.0.0",
		"canvas": "^3.2.0"
	},
	"devDependencies": {
		"@types/node": "^20.x",
		"rimraf": "^5.0.0",
		"typescript": "^4.0.0",
		"vitest": "^4.0.3"
	},
	"main": "dist/index.js"
}
