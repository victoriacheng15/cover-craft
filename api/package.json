{
	"name": "api",
	"version": "1.0.0",
	"description": "",
	"scripts": {
		"copy:assets": "mkdir -p dist/assets && cp -r src/assets/fonts dist/assets/fonts",
		"build": "tsc && npm run copy:assets",
		"watch": "tsc -w",
		"clean": "rm -rf dist",
		"prestart": "npm run clean && npm run build",
		"start": "func start",
		"test": "vitest run",
		"test:watch": "vitest --watch",
		"test:coverage": "vitest run --coverage"
	},
	"dependencies": {
		"@azure/functions": "^4.14.0",
		"@azure/storage-queue": "^12.29.0",
		"@cover-craft/shared": "file:../shared",
		"canvas": "^3.2.2",
		"mongoose": "^9.5.0"
	},
	"devDependencies": {
		"@types/node": "^25.x",
		"@vitest/coverage-v8": "^4.1.0",
		"typescript": "^6.0.3",
		"vitest": "^4.1.5"
	},
	"main": "dist/index.js"
}
